[package]
name = "jsonapi-showcase-resources"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
jsonapi_core = { path = "../../jsonapi_core", version = "1.0.0-rc.1" }
serde = { version = "1", features = ["derive"] }
validator = { version = "0.20", features = ["derive"] }

[lints]
workspace = true
