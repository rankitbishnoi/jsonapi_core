//! End-to-end acceptance tests for `jsonapi_core`.
//!
//! Tests live in `tests/` and exercise realistic JSON:API payloads as a
//! downstream consumer would.
//!
//! This library target is intentionally empty — the crate exists to host
//! the `tests/` directory in workspace layout.
